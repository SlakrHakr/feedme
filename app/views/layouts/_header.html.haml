#header
  %a.no-style.hefty-text{ href: root_path }
    = fa_icon 'rss', text: 'FEEDme', right: true
  .actions
    - if user_signed_in?
      = link_to('Logout', destroy_user_session_path, method: :delete, class: 'no-style')
      |
      = current_user.username
